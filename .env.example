# Production origin with NO trailing slash. Canonical URLs, JSON-LD, and Open Graph use this at runtime.
# Match the hostname you verify in Google Search Console.
VITE_SITE_URL=https://brandpanther.ai
